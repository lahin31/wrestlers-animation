import React from 'react';
import "./navbar.scss";

const Navbar = props => (
    <header className="header">
        <nav className="navbar">
            <div className="container">
                <h2>Wrestler Animation</h2>
            </div>
        </nav>
    </header>
);

export default Navbar;